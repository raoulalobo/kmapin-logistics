{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx zenstack generate && npx prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["cdg1"]
}
