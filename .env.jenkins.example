# Configuration Jenkins pour le script de validation
# Copiez ce fichier vers .env.jenkins et remplissez les valeurs

# URL de votre serveur Jenkins
# Exemples :
#   - http://localhost:8080 (local)
#   - http://54.123.45.67:8080 (IP publique)
#   - https://jenkins.fasofret.com (domaine avec HTTPS)
JENKINS_URL=http://localhost:8080

# Nom d'utilisateur Jenkins (généralement 'admin')
JENKINS_USER=admin

# API Token Jenkins
# Pour générer un token :
#   1. Aller dans Jenkins → User → Configure
#   2. Section "API Token" → Add new Token
#   3. Copier le token généré
#   4. Coller ci-dessous
JENKINS_TOKEN=your-api-token-here

# Nom du job Pipeline (doit correspondre au nom dans Jenkins)
JENKINS_JOB_NAME=kmapin-logistics-v2-pipeline

#########################################
# INSTRUCTIONS D'UTILISATION
#########################################

# 1. Créer un API Token dans Jenkins :
#    Jenkins → Cliquer sur votre nom (en haut à droite) → Configure
#    → Section "API Token" → Add new Token → Nom: "CLI Token"
#    → Generate → Copier le token affiché

# 2. Copier ce fichier :
#    cp .env.jenkins.example .env.jenkins

# 3. Éditer .env.jenkins et remplir les valeurs :
#    nano .env.jenkins  # ou vim, code, etc.

# 4. Exécuter le script de validation :
#    ./scripts/validate-jenkins-setup.sh

# NOTES :
# - NE PAS committer .env.jenkins (contient des secrets)
# - .env.jenkins est déjà dans .gitignore
# - Le token est équivalent à votre mot de passe Jenkins
